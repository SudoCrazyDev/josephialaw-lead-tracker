services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Use .env if present on server; or create .env.production and set env_file to that
    env_file:
      - .env
    restart: unless-stopped
